{
  "name": "shubhastro-automation",
  "version": "1.0.0",
  "description": "Playwright automation framework for Shubhastro",
  "scripts": {
    "test": "npx rimraf allure-results allure-report && npx playwright test",
    "test:headed": "npx rimraf allure-results allure-report && npx playwright test --headed",
    "test:chrome": "npx rimraf allure-results allure-report && npx playwright test --project=chromium",
    "test:firefox": "npx rimraf allure-results allure-report && npx playwright test --project=firefox",
    "test:safari": "npx rimraf allure-results allure-report && npx playwright test --project=webkit",
    "test:debug": "npx playwright test --debug",
    "test:geo": "npx rimraf test-results/geo-new-vs-current-report.jsonl && npx playwright test geo- --project=chromium && node utils/geoReport.js",
    "geo:report": "node utils/geoReport.js",
    "report": "npx allure serve allure-results",
    "report:generate": "npx allure generate allure-results --clean -o allure-report",
    "clean:results": "npx rimraf allure-results allure-report"
  },
  "dependencies": {},
  "devDependencies": {
    "@playwright/test": "^1.45.0",
    "allure-playwright": "^3.0.0",
    "allure-commandline": "^2.29.0",
    "dotenv": "^16.4.5",
    "mongodb": "^6.8.0",
    "rimraf": "^5.0.7"
  }
}
